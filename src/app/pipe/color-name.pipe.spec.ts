import { ColorNamePipe } from './color-name.pipe';

describe('ColorNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorNamePipe();
    expect(pipe).toBeTruthy();
  });
});
